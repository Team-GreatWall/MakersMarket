@model IEnumerable<MakersMarket.Models.Category>

@{
    ViewBag.Title = "Index";
    Layout = "../Shared/_Layout.cshtml";

    var pagedList = (IPagedList)Model;
}
@using PagedList.Mvc;
@using PagedList;

<div id="admin-add-button" class="form-group">
    @Html.ActionLink("Create New", "Create", "Category", routeValues: null, htmlAttributes: new { @class = "btn btn-primary" })
</div>
<div class="row">
    <div class="col-xs-4">@Html.DisplayNameFor(model => model.Name)</div>
    <div class="col-xs-4">Редакция</div>
</div>
@foreach (var item in Model) {
    <div class="row" style="border-top: 2px solid slategray; background-color: #f9f9f9">
        <div style="margin-bottom: 45px; margin-top: 9px">

            <div class="col-xs-4">
                @Html.DisplayFor(modelItem => item.Name)
            </div>
            <div class="col-xs-4">
                @Html.Partial("_ItemActions", new ViewDataDictionary { { "ItemID", item.Id }, { "ItemType", "Category" } })
            </div>
        </div>
    </div>
}
@Html.PagedListPager(pagedList, page => Url.Action("Index", new { page }))